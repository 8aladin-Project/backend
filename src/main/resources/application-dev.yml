spring:
  application:
    name: dev-backend
  config:
    activate:
      on-profile: dev   # dev 프로필이 활성화될 때만 이 파일 내용이 적용

  # H2 데이터베이스 설정 (개발 환경용)
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  
  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop  # 애플리케이션 시작 시 테이블 생성, 종료 시 삭제
    show-sql: true
    properties:
      hibernate:
        highlight_sql: true
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect

  # Redis 설정
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      timeout: 2000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0

# Infisical 설정 (Dev 환경)
# dev 환경에서는 자동으로 CLI 방식 사용
# 프로젝트 ID는 하드코딩
infisical:
  enabled: true  # Infisical 사용 활성화
  environment: dev
  project-id: 5936eede-5b19-4d5f-84aa-e48380617257  # 프로젝트 ID 하드코딩
  secrets-path: /

# AWS S3 설정 (Dev 환경에서는 비활성화)
cloud:
  aws:
    s3:
      enabled: false  # Dev 환경에서는 S3 비활성화
